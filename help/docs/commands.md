# Commands

*Please note that all commands have prompts displayed on the CommandLine. If in doubt read the CommandLine.*

## Keyboard Shortcuts

Below is list of common keyboard commands available in Design. Enter shortcut key followed by SpaceBar or Enter.

Shortcut | Command Description
:---     |:---
`L`      | **Line** Pick a start point followed by picking consecutive point(s) to draw line(s)
`PL`     | **PolyLine** Pick a start point followed by picking consecutive point(s) to draw a continuos Polyline
`C`      | **Circle** Pick a centre point followed by second point to define the Radius
`x`      | **x**
`x`      | **x**
`x`      | **x**
